import React from 'react';
import _ from 'lodash';
import { useParams } from 'react-router-dom';


function AboutTrail(props) {

  //let trailName = ''; //REPLACE THIS WITH CORRECT VALUE
  //const urlParams = useParams();
  //trailName = urlParams.trailname;

  //find the trail from the json file for trail information

  return (
    <div>
      <h2>Wow it's the trail info page.</h2>
    </div>
  );
}

export default AboutTrail;